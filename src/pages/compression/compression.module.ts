import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CompressionPage } from './compression';

@NgModule({
  declarations: [
    CompressionPage,
  ],
  imports: [
    IonicPageModule.forChild(CompressionPage),
  ],
})
export class CompressionPageModule {}
